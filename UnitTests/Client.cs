using Xunit;
using Moq;

namespace UnitTests
{
    public class Client
    {
        [Fact]
        public void AddShouldReturnSumWhenGivenTwoNumbers()
        {
            // Arrange
            //var mockCalculator = new Mock<ICalculator>();
            //mockCalculator.Setup(calc => calc.Add(2, 3)).Returns(5); // Set up the mock

            // Act
            //int result = mockCalculator.Object.Add(2, 3);

            // Assert
            //Assert.Equal(5, result);
        }
    }
}